import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sanitas.app',
  appName: 'Sanitas-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
